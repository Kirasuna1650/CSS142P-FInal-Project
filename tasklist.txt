Atlas Academy to Retention Task List

Goal: use Atlas Academy servant metadata to inform retention-related formulas in the CSS142P notebook without turning the project into a full game-data reimplementation.

[ ] Step 1: Confirm the exact Atlas Academy export(s) to use.
- Use `basic_servant.json` for lightweight servant identity and classification fields.
- Use `nice_servant.json` only if richer release/status fields are needed.
- Decide whether the notebook should read from a live URL, a downloaded local file, or both.

[ ] Step 2: Define the servant features that matter for retention logic.
- Keep only fields that can support analysis, such as class, rarity, limited status, story-lock signals, NPC-only flags, and release window indicators.
- Decide which fields are descriptive only and should not affect formulas.
- Write down the final feature list before changing code.

[ ] Step 3: Add a loader in the notebook for Atlas Academy servant data.
- Load JSON from the selected source.
- Normalize the records into a tabular DataFrame.
- Handle nested or missing fields safely.

[ ] Step 4: Build a servant metadata summary table.
- Group servants by class and rarity.
- Count limited, story-locked, and NPC-only servants.
- Export or display the summary so it can be reviewed quickly.

[ ] Step 5: Map servant metadata to retention-relevant inputs.
- Define how rarity, limited availability, and release constraints influence acquisition difficulty or player frustration.
- Decide whether the metadata changes the base pull rate, the frustration score, the pity trigger, or the retention probability.
- Keep the mapping simple enough to explain in the final report.

[ ] Step 6: Update the retention formulas.
- Add variables derived from Atlas Academy metadata.
- Make sure the formulas still work with synthetic player segments.
- Keep the model comparative rather than trying to replicate the live game exactly.

[ ] Step 7: Add scenario controls.
- Create a baseline scenario and at least two alternatives that reflect different assumptions about servant availability or acquisition friction.
- Keep scenario names clear and reportable.
- Make sure every scenario uses the same random-seed procedure.

[ ] Step 8: Add verification checks for the new data path.
- Verify the loader returns data in the expected shape.
- Verify the normalization helper returns the required columns.
- Verify the summary table groups and counts correctly.
- Verify the retention formulas still produce values in valid ranges.

[ ] Step 9: Add validation checks.
- Compare the simplified metadata rules with visible Atlas Academy fields.
- Check extreme cases, such as always-limited or never-limited inputs.
- Confirm the formulas do not create impossible probabilities.

[ ] Step 10: Run the notebook end to end.
- Execute from imports through the Atlas Academy section.
- Execute the simulation and scenario comparison cells.
- Regenerate the CSV outputs and the diagram.

[ ] Step 11: Review the results for report use.
- Capture the outputs that show acquisition rate, frustration, retention, and sensitivity behavior.
- Note any assumptions or limitations caused by using public metadata rather than internal game data.
- Record the exact Atlas Academy fields used in the write-up.

[ ] Step 12: Clean up reproducibility details.
- Document the API URL or local file path used.
- Record any package or kernel requirements.
- Keep the random-seed procedure and output file names in the notebook or README.

[ ] Step 13: Update the markdown draft if needed.
- Add a short data section explaining how servant metadata informs the model.
- Add a limitation note that the metadata is used as a proxy, not as a direct commercial retention signal.
- Make sure the report language matches the implemented notebook behavior.
